sudo: required
dist: xenial
language: python
addons:
  apt:
    packages:
    - direnv
env:
  matrix:
  - PY_VERSION=2.7.10
  - PY_VERSION=2.7.12
  - PY_VERSION=2.7.15
  - PY_VERSION=3.3.0
  - PY_VERSION=3.4.0
  - PY_VERSION=3.7.0
before_script:
- curl -L https://github.com/pyenv/pyenv-installer/raw/master/bin/pyenv-installer | bash
script:
- pyenv versions
- which direnv
